export declare class LevelOverlayInfo {
    x: number;
    y: number;
    width: number;
    height: number;
    outputs: [number, number, number][];
    constructor(x: number, y: number, width: number, height: number, outputs: [number, number, number][]);
}
