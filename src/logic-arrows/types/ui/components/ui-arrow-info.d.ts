import { UIComponent } from './ui-component';
export declare class UIArrowInfo extends UIComponent {
    constructor(parent: HTMLElement, index: number);
    getClass(): string;
    private replaceInlineColors;
}
